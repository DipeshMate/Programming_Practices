#include <iostream>
#include <cstring>

using namespace std;

int main()
{
    // int num, d, rev = 0;
    // cin >> num;

    // int temp = num;

    // while (temp != 0)
    // {
    //     d = temp % 10;
    //     rev = rev * 10 + d;
    //     temp /= 10;
    // }

    // int d, num, rev = 0;
    // cin >> num;
    // int temp = num;
    // while (temp != 0)
    // {
    //     d = temp % 10;
    //     rev = rev * 10 + d;
    //     temp /= 10;
    // }
    // cout << rev;
    // char arr[10];
    // cin >> arr;
    // strrev(arr);
    // cout << "reverse number is " << arr;

    return 0;
}